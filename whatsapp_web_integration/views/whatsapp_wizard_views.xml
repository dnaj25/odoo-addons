<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View for WhatsApp Wizard -->
    <record id="view_whatsapp_message_wizard_form" model="ir.ui.view">
        <field name="name">whatsapp.message.wizard.form</field>
        <field name="model">whatsapp.message.wizard</field>
        <field name="arch" type="xml">
            <form string="Send WhatsApp Message">
                <sheet>
                    <group>
                        <group>
                            <field name="partner_id" readonly="1" options="{'no_open': True}"/>
                            <field name="phone"/>
                        </group>
                    </group>
                    <label for="message"/>
                    <field name="message" placeholder="Type your message here..." widget="text" style="height: 150px;"/>
                </sheet>
                <footer>
                    <button name="action_send_whatsapp" string="Send via WhatsApp" type="object" class="oe_highlight"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to launch Wizard -->
    <record id="action_whatsapp_message_wizard" model="ir.actions.act_window">
        <field name="name">Send WhatsApp Message</field>
        <field name="res_model">whatsapp.message.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
