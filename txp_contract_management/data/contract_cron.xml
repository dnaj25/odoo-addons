<odoo>
    <data noupdate="0">
        <record id="ir_cron_contract_expiry_check" model="ir.cron">
            <field name="name">Contract Expiry Check &amp; Alerts</field>
            <field name="model_id" ref="model_contract_contract"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_contract_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(datetime.utcnow().replace(hour=15, minute=40, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>